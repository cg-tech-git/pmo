COPY THESE VALUES TO YOUR .env.local FILE:

Replace the existing Firebase variables in your .env.local file with:

NEXT_PUBLIC_FIREBASE_API_KEY=AIzaSy... (your actual API key)
NEXT_PUBLIC_FIREBASE_AUTH_DOMAIN=your-project.firebaseapp.com
NEXT_PUBLIC_FIREBASE_PROJECT_ID=your-project-id
NEXT_PUBLIC_FIREBASE_STORAGE_BUCKET=your-project.appspot.com
NEXT_PUBLIC_FIREBASE_MESSAGING_SENDER_ID=123456789
NEXT_PUBLIC_FIREBASE_APP_ID=1:123456789:web:abc123

Keep your existing GCP variables:
GCP_PROJECT_ID=pmo-documents-hybrid-shine-466111-s0
GCS_BUCKET_NAME=pmo-documents-hybrid-shine-466111-s0

INSTRUCTIONS:
1. Get the values from Firebase Console → Project Settings → Your apps
2. Replace each "your-project" with your actual Firebase project values
3. Save the .env.local file
4. The development server will automatically restart
5. Visit http://localhost:3000 to test authentication 